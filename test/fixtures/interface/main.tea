init()

static async function SATest(a: string): string {
  var subResourcesMap : map[string]string = {};
  var key : string = 'key';
  subResourcesMap[key] = 'value'; 
  return a;
}

async function STest(a: string): string {
  return a;
}

function Test(a: string): string {
  return a;
}