import Assert;
import Credential;

static function test(): void {
  var config = new Credential.Config;
  var credential = new Credential(config);
  Assert.equal('access_key_id', credential.getAccessKeyId(), '');
  Assert.equal('access_key_secret', credential.getAccessKeySecret(), '');
}

