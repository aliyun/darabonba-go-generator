
model RuntimeObject = {
  maxRetry: number,
}


model Config extends RuntimeObject {
  maxAttemp: number,
}


init(config: Config);
function print(runtime: RuntimeObject): void {
  return;
}

model Request = {
  accesskey?: string(name='accesskey', description='accesskey'),
  region?: string(name='region', description='region'),
}