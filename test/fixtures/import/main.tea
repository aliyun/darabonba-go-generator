import Source;

init();

type @sourceClient = Source;

function Sample(client: Source): void {
    var runtime = new Source.RuntimeObject{};
    var request = new Source.Request{
        accesskey = "accesskey",
        region = "region",
    };
    client.print(runtime);
    @sourceClient.print(runtime);
}
